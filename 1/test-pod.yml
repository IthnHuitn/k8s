apiVersion: v1
kind: Pod
metadata:
  name: test-multitool
  labels:
    app: test-multitool
spec:
  containers:
  - name: multitool
    image: wbitt/network-multitool:minimal
    command: ["sleep", "3600"]
    resources:
      limits:
        memory: "128Mi"
        cpu: "100m"